{
  "name": "knox",
  "version": "2.3.2",
  "description": "Security enforcement layer for Claude Code. Blocks dangerous commands, audits every tool call, detects prompt injection.",
  "author": { "name": "Qoris", "email": "apps@qoris.ai", "url": "https://qoris.ai" },
  "homepage": "https://qoris.ai/knox",
  "repository": "https://github.com/qoris-ai/knox",
  "license": "MIT",
  "keywords": ["security", "enforcement", "audit", "safety", "policy"],
  "userConfig": {
    "preset": {
      "title": "Security preset",
      "description": "Type one of: paranoid | strict | standard | minimal | disabled. Default 'standard'. Easier than this text field: run `/knox:preset <name>` in any Claude Code session, or `knox preset <name>` from a terminal — both override this value. (Claude Code 2.1.x renders this as a text input; checkbox UI for boolean userConfig isn't shipped yet.)",
      "type": "string",
      "default": "standard"
    },
    "webhook": {
      "title": "Webhook URL",
      "description": "Webhook URL for security alerts (optional, leave blank to skip)",
      "type": "string",
      "sensitive": false
    },
    "audit_path": {
      "title": "Audit Log Path",
      "description": "Audit log directory path (leave blank for default: ~/.local/share/knox/audit)",
      "type": "string"
    }
  }
}
